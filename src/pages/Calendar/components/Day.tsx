interface DateItem {
  year: number
  month: number
  date: number
  active: boolean
}

const Day = (index: number, date: DateItem, click: (date: Date) => void) => {
  return <></>
}

export default Day
